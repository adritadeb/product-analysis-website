import React from 'react';

const BlogAns2 = () => {
    return (
        <div>
            <h2 className='text-2xl mb-4 mt-9'>2. What is Semantic tag?</h2>
            <p className='mx-4'>-- The semantic tag describes how information looks on the UI. It contains many meaningful HTML tags. By using those tags, one can easily understand the purpose of the code. The browser can also easily understand the meaning of the information inside the tags. There are particular semantic tags for the particular sections of UI. For example - header tag, nav tag, section tag, aside tag, footer tag, etc.</p>
        </div>
    );
};

export default BlogAns2;